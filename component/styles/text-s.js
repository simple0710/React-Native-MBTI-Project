import { StyleSheet } from "react-native";

const text = StyleSheet.create({
  upload: {
    flex: 1,
    textAlign: "center",
    fontSize: 18,
    fontWeight: "bold",
  },
  check: {
    flex: 1,
  },
})

export default text;